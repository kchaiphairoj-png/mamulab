# Prosperity page images

Drop the 4 product photos into this folder with **these exact filenames**:

| filename | what | source |
|---|---|---|
| `wallets-lineup.jpg` | BeRichy 5 colors line-up (the brand banner) | first image you sent |
| `wallet-detail.jpg` | brown + red interior/exterior collage | second image |
| `coin-faces.jpg` | coin front (พระประดิษฐาน) + back (Enochian sigil) | third image |
| `coin-box.jpg` | coin in black box with gold ritual paper | fourth image |

After dropping the files here:

1. Commit + push (or just `git add public/prosperity/*.jpg && git commit -m "Add prosperity photos" && git push`)
2. Vercel auto-deploys
3. The photos appear on https://mamulab.com/prosperity

Recommended sizes:
- 1080×1080 or higher (the layout crops to fit each aspect ratio)
- JPG, quality 80-90%
- Total under 500 KB each for fast loading
